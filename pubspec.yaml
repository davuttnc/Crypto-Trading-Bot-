name: dubai_bot_mobil
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.1.0
  fl_chart: ^0.66.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  syncfusion_flutter_charts: ^27.1.48
  candlesticks: ^2.1.0
  
  # Haberler için gerekli paketler
  webfeed_plus: ^1.1.2  # RSS feed parser (düzeltilmiş sürüm)
  url_launcher: ^6.3.1  # Haberleri tarayıcıda açmak için

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true